import { HoverBgDirective } from './hover-bg.directive';

describe('HoverBgDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverBgDirective();
    expect(directive).toBeTruthy();
  });
});
